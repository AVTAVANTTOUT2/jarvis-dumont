ECHO-02C reuses the existing VoiceLoop and configured offline AudioEngine via RemoteEchoAudio and an inherited binary PCM pipe; never add an EchoVoiceLoop or choose an Echo-specific STT model. The isolated Echo gateway supports ETHERNET_REQUIRED with explicit IPv4/interface, IP_BOUND_IF and fail-closed link monitoring. Frozen reference: mono PCM16, uplink16k/downlink48k,20ms,MEDIA usage1,prefill100ms; Android AudioIo remains unchanged. One assistant turn owns one downlink stream across TTS segments; session/uplink/turn identities reject late PCM. OFF reaches the client before worker drain; reconnect needs explicit activation. ACTIVE conversation treats every accepted utterance as a request, including speech that does not start with Jarvis. Normal ACTIVE/PASSIVE modes have no turn quota: five-minute capture windows renew on the same authorized connection and workers until explicit OFF, disconnect or error; the one-turn diagnostic smoke remains bounded. The default terminal-silence endpoint is 1500 ms so a natural one-second pause does not split a long phrase. PASSIVE context is bounded RAM and ephemeral per request turn; non-addressed PASSIVE speech still does not call DeepSeek. Clear invalidates in-flight old-generation passive text. Persistent memory is a separate owner setting: on ACTIVE/PASSIVE it restores the bounded summary plus unsummarized confirmed turns; rollups use the nominal budget, are cancelled if the user speaks, and never retry. clear_memory is distinct from clear_context and does not delete stored conversations. Qualification uses a separate persisted request budget, default eight and explicitly configurable up to ten with user authorization, preserving consumed attempts across a ceiling change, and the common Office instance lock. Installed main/STT/TTS wheels must match; preserve configured models, worker interpreter/dependency versions, main current and LaunchAgent. Use src/jarvis_office/echo/ECHO-02C.md for implementation/run contracts. Never seize the Echo from an armed paired-capture campaign. Functional remote success does not qualify STT or secure DEV_INSECURE_LAN.

Production and configured DeepSeek request is deepseek-flash (DeepSeek-V4.1-Flash), non-thinking. Live private TOMLs and Chat.model default must send that name; the parser still accepts returned deepseek-flash and the retired deepseek-v4-flash* identifiers, records the actual returned name, and rejects Pro/unknown names or a model change within one stream. Do not send deepseek-v4-pro or the retired request name. Unrelated to STT qualification. Private release 0.3.0-90e679016c44 is the activated Flash V4.1 candidate; previous 0.3.0-8b72c208029f remains available.

Remote pacing must rebase after a producer gap rather than flush overdue frames in a burst. The 24 kB source credit includes in-flight PCM and is drained by one owned sender task, allowing the next TTS segment to begin synthesis before the previous segment finishes playback. Abort cancels and awaits that sender, then clears queued PCM; stale sessions never replay it. Keep Android audio settings fixed when correcting this server scheduling path. Source queue residence belongs in PCM-ready-to-send metrics, not in a claimed zero-latency shortcut.

The Android debug dump retains only the latest playback trace and a process-lifetime failure ring. A delayed observer can therefore save the same trace under two server turn names; correlate the trace timestamps/stream before claiming per-turn evidence, and do not count old failure-ring entries as new failure in a later stability window. After-drain underrun increments are distinct from starvation while data was expected.
